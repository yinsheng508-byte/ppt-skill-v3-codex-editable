# 用户确认_阶段1

## 项目目标

（由主控大模型填写用户目标、受众、使用场景、演示时长和最终交付形式。）

## 资料整合摘要

（由主控大模型简要说明本次参考了哪些 PPT、Word、PDF、截图、表格、讲稿或用户口径。完整逐页资料依据写入 `页面规划.md`，不要在此重复完整页面规划。）

## 风格与提示词方案

阶段1风格和图片提示词概要合并见：

```text
阶段1_规划确认/风格与提示词方案.md
```

（由主控大模型填写初步模板方向、视觉风格、颜色气质、参考风格、全局提示词方向和逐页提示词概要。最终视觉系统会在阶段2A用户选择四张封面之一后锁定。）

## PPT路线

（由主控大模型判断主路线和副路线：课件型 / 护理宣教型 / 学术答辩型 / 创赛路演型 / 专业汇报型 / 演讲发布型 / 资料改造型 / 仿做升级型。）

## 页面规划入口

完整逐页规划见：

```text
阶段1_规划确认/页面规划.md
```

`页面规划.md` 必须包含页码、页面角色、页面标题、页面目的、最终可见中文文字、资料依据、内容保留边界和视觉意图；这里的页码是规划索引，不代表画面上要显示可见页码。

## 每页干净逐字稿入口

方便逐页审阅正文内容的干净阅读稿见：

```text
阶段1_规划确认/每页干净逐字稿.md
```

`每页干净逐字稿.md` 必须按页分段，用标题、要点、表格、流程等 Markdown 格式排好每页最终可见文字，并与 `页面规划.md`、`content.json` 和 `slide_prompt_briefs.json` 逐页一致。

## 设计合同

机器态设计合同见：

```text
_state/阶段1/design_contract.json
```

它记录路线、受众、资料边界、内容改写边界、审美方向、页面角色和阶段2封面候选策略。

## 请用户确认

请确认 `页面规划.md` 中的页数、页面角色、每页最终可见中文文字、资料依据、内容保留边界，`每页干净逐字稿.md` 中逐页内容是否好读且准确，`风格与提示词方案.md` 中的风格和提示词方向，以及本文件中的项目目标。确认后进入阶段2A，先生成四张封面风格候选供你选择。
