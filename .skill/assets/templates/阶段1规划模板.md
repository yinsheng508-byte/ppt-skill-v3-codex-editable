# 阶段1规划确认总览

## 项目目标

（由主控大模型填写用户目标、受众、使用场景、演示时长和最终交付形式。）

## 资料整合摘要

（由主控大模型简要说明本次参考了哪些 PPT、Word、PDF、截图、表格、讲稿或用户口径。完整逐页资料依据写入 `页面规划.md`，不要在此重复完整页面规划。）

## PPT一致性

阶段1标题、模块页、公共元素和例外规则见：

```text
阶段1_规划确认/PPT一致性.md
```

（由主控大模型填写具体一致性规则，后续每页图片提示词会读取本项目文档，不读取 reference 原文。）

## 图片风格

阶段1风格和图片提示词概要见：

```text
阶段1_规划确认/图片风格.md
```

（由主控大模型填写当前配色、质感、字体、必要禁用项及各页应用。阶段2A在这个共同风格范围内提供四种封面构图，用户选择后进一步细化内页规则。）

## PPT路线

（由主控大模型判断主路线和副路线：课件型 / 护理宣教型 / 学术答辩型 / 创赛路演型 / 专业汇报型 / 演讲发布型 / 资料改造型 / 仿做升级型。）

## 页面规划入口

完整逐页规划见：

```text
阶段1_规划确认/页面规划.md
```

`页面规划.md` 必须包含页码、页面角色、页面标题、页面目的、最终可见中文文字、资料依据、内容保留边界和视觉意图；这里的页码是规划索引，不代表画面上要显示可见页码。

## 每页干净逐字稿入口

方便逐页审阅正文内容的干净阅读稿见：

```text
阶段1_规划确认/每页干净逐字稿.md
```

`每页干净逐字稿.md` 必须按页分段，用标题、要点、表格、流程等 Markdown 格式排好每页最终可见文字，并与 `页面规划.md`、`content.json` 和 `slide_prompt_briefs.json` 逐页一致。

## 设计合同

机器态设计合同见：

```text
_state/阶段1/design_contract.json
```

它记录路线、受众、资料边界、内容改写边界、审美方向、页面角色和阶段2封面候选策略；这些设计依据不直接作为生成指令发送。

## 确认口径

阶段1只确认 `页面规划.md`、`每页干净逐字稿.md`、`PPT一致性.md` 和 `图片风格.md` 四份正式材料。本总览只帮助主控汇总项目目标和材料关系，不作为额外确认文件。确认阶段1后进入阶段2A，先生成四张封面风格候选供用户选择。
